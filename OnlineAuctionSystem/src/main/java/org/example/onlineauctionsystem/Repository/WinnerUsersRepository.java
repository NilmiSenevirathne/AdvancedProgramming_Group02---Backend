package org.example.onlineauctionsystem.Repository;

import org.example.onlineauctionsystem.Entity.WinnerUsers;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WinnerUsersRepository extends JpaRepository<WinnerUsers , Long> {
}
